kind = "web"
previewPath = "/"
title = "Dev Studio Landing"
version = "1.0.0"
id = "artifacts/dev-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21531

[services.development]
run = "pnpm --filter @workspace/dev-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dev-studio", "run", "build" ]
publicDir = "artifacts/dev-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21531"
BASE_PATH = "/"
